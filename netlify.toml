# example netlify.toml
[build]
  command = "npm install && npx elm make src/Main.elm --output elm.js"
  functions = "functions"
  publish = "."

[dev]
  command = "npx elm-live src/Main.elm"
  targetPort = 8000
  publish = "dist"
  autoLaunch = true
  framework = "#custom"
